import React from "react";
import { Card } from "antd";

let RoleList: React.FC = () => {
    return (
        <div className="role-list-page">
            <Card hoverable>role page</Card>
        </div>
    );
};

export default RoleList;
